
# ---------   Numpy   ------>   In folder   ----------------------------------------------------------------------------

# ---------   Sympy   ------>   In folder   ----------------------------------------------------------------------------

# ---------   Built in Functions   -------------------------------------------------------------------------------------

fruits = ["apple", "banana", "cherry"]
numbers = range(3)  # Generates a sequence of numbers 0,1,2.

print("Hello, Python!")  # Outputs text or values to the console.
input("Enter your name: ")  # Reads input from the user.

str(42)  # Convert values to strings
int("42")  # Convert values to integers
float("3.14")  # Convert values to floating-point numbers.

list(numbers)  # Convert sequences to lists.
tuple(numbers)  # Convert sequences to tuples.
set(numbers)  # Convert sequences to sets.
sorted(numbers)  # Returns a sorted version of a sequence.

len(numbers)  # Returns the length of a string, list, or any iterable.
max(numbers)  # Returns the maximum and minimum value in a sequence.
min(numbers)  # Returns the maximum and minimum value in a sequence.
sum(numbers)  # Returns the sum of all elements in a sequence.

enumerate(fruits)  # Generates index-value pairs from a sequence.
zip(numbers, fruits)  # Combines multiple sequences element-wise.
dict(zip(numbers, fruits))  # Creates a dictionary from key-value pairs.
